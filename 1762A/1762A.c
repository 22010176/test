#include <stdio.h>
#include <math.h>
#include <string.h>
#include <stdlib.h>
int solve() {

}
int main() {
  int test;
  scanf("%d", &test);

  int result[test];
  for (int i = 0; i < test;i++) {
    int len;
    scanf("%d", &len);
    int a[len];
    for (int j = 0;j < len;j++) {
      scanf("%d", a + j);
    }

  }
}